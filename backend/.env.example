DATABASE_URL="postgresql://webgenerica:webgenerica_dev@localhost:5433/webgenerica?schema=public"
JWT_SECRET="change-this-to-a-random-secret-in-production"
PORT=3001
# Comma-separated origins for CORS (e.g. http://localhost:5173,https://webgenerica.vercel.app)
FRONTEND_URL="http://localhost:5173"
UPLOAD_DIR="./uploads"

# Super Admin credentials (used by seed)
SUPER_ADMIN_EMAIL="superadmin@webgenerica.com"
SUPER_ADMIN_PASSWORD="superadmin123"

# Public URL of the backend (for activation email links)
APP_URL="http://localhost:3001"

# SMTP for emails (activation, notifications)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""
